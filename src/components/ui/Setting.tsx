import "./setting.css";

export interface SettingProps {}

const Setting = () => {
  return (
    <ul className="setting">
      <li className="setting-item"> 설정 </li>
    </ul>
  );
};

export default Setting;
